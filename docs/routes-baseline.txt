GET     /api/swagger   [rateLimit]
GET     /api/docs   [rateLimit]
GET     /api/health   [rateLimit]
GET     /api/auth/status   [rateLimit]
POST    /api/auth/register   [rateLimit,rateLimit]
POST    /api/auth/login   [rateLimit,rateLimit]
GET     /api/auth/me   [rateLimit,auth]
POST    /api/auth/logout   [rateLimit,auth]
POST    /api/connections/discover-databases   [rateLimit,auth,rateLimit]
GET     /api/connections/docker-hosts   [rateLimit,auth]
GET     /api/connections   [rateLimit,auth]
POST    /api/connections   [rateLimit,auth]
GET     /api/connections/:id   [rateLimit,auth]
PUT     /api/connections/:id   [rateLimit,auth]
PATCH   /api/connections/:id   [rateLimit,auth]
DELETE  /api/connections/:id   [rateLimit,auth]
GET     /api/storage-destinations   [rateLimit,auth]
POST    /api/storage-destinations   [rateLimit,auth]
GET     /api/storage-destinations/:id   [rateLimit,auth]
PUT     /api/storage-destinations/:id   [rateLimit,auth]
PATCH   /api/storage-destinations/:id   [rateLimit,auth]
DELETE  /api/storage-destinations/:id   [rateLimit,auth]
GET     /api/storage-destinations-space   [rateLimit,auth]
GET     /api/storage-destinations/:id/space   [rateLimit,auth]
GET     /api/storages   [rateLimit,auth]
POST    /api/storages   [rateLimit,auth]
GET     /api/storages/copy-jobs/:jobId   [rateLimit,auth]
GET     /api/storages/archive-jobs/:jobId   [rateLimit,auth]
GET     /api/storages/archive-jobs/:jobId/download   [rateLimit,auth]
GET     /api/storages/:id   [rateLimit,auth]
PUT     /api/storages/:id   [rateLimit,auth]
DELETE  /api/storages/:id   [rateLimit,auth]
POST    /api/storages/:id/test   [rateLimit,auth,rateLimit]
GET     /api/storages/:id/browse   [rateLimit,auth]
DELETE  /api/storages/:id/object   [rateLimit,auth]
POST    /api/storages/:id/copy   [rateLimit,auth,rateLimit]
POST    /api/storages/:id/archive   [rateLimit,auth,rateLimit]
POST    /api/connections/:id/test   [rateLimit,auth,rateLimit]
POST    /api/connections/:id/create-database   [rateLimit,auth,rateLimit]
POST    /api/connections/:id/backup   [rateLimit,auth,rateLimit]
GET     /api/backups   [rateLimit,auth]
GET     /api/connections/:connectionId/backups   [rateLimit,auth]
GET     /api/backups/:id   [rateLimit,auth]
GET     /api/backups/:id/download   [rateLimit,auth]
POST    /api/backups/:id/restore   [rateLimit,auth,rateLimit]
DELETE  /api/backups/:id   [rateLimit,auth]
POST    /api/backups/import   [rateLimit,auth,rateLimit]
GET     /api/stats   [rateLimit,auth]
GET     /api/system/status   [rateLimit,auth]
GET     /api/system/diagnostics   [rateLimit,auth]
GET     /api/system/diagnostics/:name/download   [rateLimit,auth,rateLimit]
DELETE  /api/system/diagnostics/:name   [rateLimit,auth,rateLimit]
GET     /api/system/containers/resources   [rateLimit,auth]
GET     /api/system/resources/history   [rateLimit,auth]
GET     /api/system/backup-retention   [rateLimit,auth]
PUT     /api/system/backup-retention   [rateLimit,auth,rateLimit]
POST    /api/system/backup-retention/run   [rateLimit,auth,rateLimit]
GET     /api/audit-logs   [rateLimit,auth]
GET     /api/audit-logs/stats   [rateLimit,auth]
GET     /api/audit-logs/:id   [rateLimit,auth]
GET     /api/users   [rateLimit,auth]
PATCH   /api/users/:id/status   [rateLimit,auth]
GET     /api/docker/status   [rateLimit,auth]
GET     /api/docker/containers   [rateLimit,auth]
GET     /api/docker/containers/:id   [rateLimit,auth]
GET     /api/docker/containers/:id/logs   [rateLimit,auth]
DELETE  /api/docker/containers/:id/logs   [rateLimit,auth,rateLimit]
POST    /api/docker/containers/:id/start   [rateLimit,auth,rateLimit]
POST    /api/docker/containers/:id/stop   [rateLimit,auth,rateLimit]
POST    /api/docker/containers/:id/restart   [rateLimit,auth,rateLimit]
DELETE  /api/docker/containers/:id   [rateLimit,auth,rateLimit]
GET     /api/docker/volumes   [rateLimit,auth]
GET     /api/docker/volumes/:name   [rateLimit,auth]
GET     /api/docker/volumes/:name/export   [rateLimit,auth,rateLimit]
POST    /api/docker/volumes/:name/backup   [rateLimit,auth,rateLimit]
DELETE  /api/docker/volumes/:name   [rateLimit,auth,rateLimit]
GET     /api/docker/networks   [rateLimit,auth]
GET     /api/docker/networks/:id   [rateLimit,auth]
POST    /api/docker/networks   [rateLimit,auth,rateLimit]
POST    /api/docker/networks/:id/connect   [rateLimit,auth,rateLimit]
POST    /api/docker/networks/:id/disconnect   [rateLimit,auth,rateLimit]
POST    /api/docker/diagnostics   [rateLimit,auth,rateLimit]
GET     /api/docker/diagnostics/:jobId   [rateLimit,auth]
POST    /api/docker/images/prune   [rateLimit,auth,rateLimit]
GET     /api/docker/images   [rateLimit,auth]
GET     /api/docker/images/:id   [rateLimit,auth]
DELETE  /api/docker/images/:id   [rateLimit,auth,rateLimit]
GET     /__transmit/events
POST    /__transmit/subscribe
POST    /__transmit/unsubscribe
GET     /*
