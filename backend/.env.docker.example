# =============================================================================
# CONFIGURAÇÕES DOCKER
# Copie este arquivo para .env na raiz do projeto e configure as variáveis
# =============================================================================

# Timezone e porta externa
TZ=America/Sao_Paulo
PORT=3333

# Chave de criptografia da aplicação (OBRIGATÓRIO)
# Gere com: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
APP_KEY=

# Chave para criptografia das senhas dos bancos de dados (OBRIGATÓRIO)
# IMPORTANTE: Use uma chave forte de 32 caracteres para AES-256
# Gere com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
DB_ENCRYPTION_KEY=

# Configurações de retenção de backups (em dias/semanas/meses/anos)
RETENTION_DAILY=7
RETENTION_WEEKLY=4
RETENTION_MONTHLY=12
RETENTION_YEARLY=5
